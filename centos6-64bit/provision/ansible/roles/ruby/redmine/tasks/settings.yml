- name: Settings | Default data load.
  become_user: vagrant
  command:
    bundle exec rake redmine:load_default_data
    chdir={{ redmine_dir }}
  environment:
    PATH: "{{ ruby_environment_path }}"
    RAILS_ENV: production
    REDMINE_LANG: "{{ load_default_data_lang }}"

- name: Settings | Change default language to Japanese
  become_user: vagrant
  command:
    bundle exec rails r 'Setting["default_language"]="{{ default_language }}"'
    chdir={{ redmine_dir }}
  environment:
    PATH: "{{ ruby_environment_path }}"
    RAILS_ENV: production

- name: Settings | Change display format of user name to "Last name"
  become_user: vagrant
  command:
    bundle exec rails r 'Setting["user_format"]=:{{ display_username_format }}'
    chdir={{ redmine_dir }}
  environment:
    PATH: "{{ ruby_environment_path }}"
    RAILS_ENV: production

- name: Settings | Set gravatar_enabled
  become_user: vagrant
  command:
    bundle exec rails r 'Setting["gravatar_enabled"]="{{ gravatar_enabled }}"'
    chdir={{ redmine_dir }}
  environment:
    PATH: "{{ ruby_environment_path }}"
    RAILS_ENV: production

- name: Settings | Set gravatar_default
  become_user: vagrant
  command:
    bundle exec rails r 'Setting["gravatar_default"]="{{ gravatar_default }}"'
    chdir={{ redmine_dir }}
  environment:
    PATH: "{{ ruby_environment_path }}"
    RAILS_ENV: production

- name: Settings | Set attachment and repository encoding
  become_user: vagrant
  command:
    bundle exec rails r 'Setting["repositories_encodings"]="{{ repositories_encodings }}"'
    chdir={{ redmine_dir }}
  environment:
    PATH: "{{ ruby_environment_path }}"
    RAILS_ENV: production


- name: Settings | Set attachment thumbnail to display
  become_user: vagrant
  command:
    bundle exec rails r 'Setting["thumbnails_enabled"]="{{ thumbnails_enabled }}"'
    chdir={{ redmine_dir }}
  environment:
    PATH: "{{ ruby_environment_path }}"
    RAILS_ENV: production

- name: Settings | Set default_projects_public
  become_user: vagrant
  command:
    bundle exec rails r 'Setting["default_projects_public"]="{{ default_projects_public }}"'
    chdir={{ redmine_dir }}
  environment:
    PATH: "{{ ruby_environment_path }}"
    RAILS_ENV: production

- name: Settings | Set ui_theme
  become_user: vagrant
  command:
    bundle exec rails r 'Setting={'ui_theme' => "redmine-theme-gitmike-design"}'
    chdir={{ redmine_dir }}
  environment:
    PATH: "{{ ruby_environment_path }}"
    RAILS_ENV: production

  
